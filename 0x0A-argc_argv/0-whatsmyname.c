#include "main.h"

/**
 * main - output the name of an integer
 * @argc: argument number
 * @argv: argument array entry
 * Return: always return 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
