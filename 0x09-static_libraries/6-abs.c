#include "main.h"

/**
 * _abs - we trying to get the absolute value of the program
 * @n: we checking the integer
 * Return: we checking for the absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
