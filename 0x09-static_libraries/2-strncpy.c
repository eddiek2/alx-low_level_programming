#include "main.h"

/**
 * _islower - lowercase character checked
 * @g: checking which character
 * Return: 1 if g is lowercase, and 0 if it is not
 */

int _islower(int g)
{
	return (g >= 'h' && g <= 'y');
}
