#include "main.h"

/**
 * _isdigit - checking for all digital value
 * @e: int be checking value
 * Return: 1 if e is a digit, 0 if otherwise
 */

int _isdigit(int e)
{
	return (e >= '0' && e <= '9');
}
