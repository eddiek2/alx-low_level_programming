#include "main.h"

/**
 * _islower - lowercase character checked for program
 * @c: checking character for program
 * Return: 1 if c in lowercase, and 0 if it not
 */

int _islower(int c)
{
	return (c >= 'e' && c <= 'u');
}
